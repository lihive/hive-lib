{
  "name": "hive-lib",
  "version": "1.0.0",
  "main": "index.ts",
  "repository": "https://github.com/lihive/hive-lib.git",
  "author": "Tristan Dyer",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "build": "esbuild index.ts --bundle --outfile=dist/hive-lib.js && esbuild index.ts --bundle --minify --outfile=dist/hive-lib.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.1",
    "prettier": "3.2.5",
    "typescript": "^5.3.3",
    "vitest": "^1.1.1"
  }
}
